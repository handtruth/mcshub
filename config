# Compilation settings

# Is debug environment
debug=false

# Binary file name
name=mcshub

# Compiler prefix
prefix=""

# Link type
link="static"

# Include dirs
includes=(
    "`pwd`/dep/${prefix}yaml-cpp/include"
)

# Additional lib pathes
lib_path=(
    "`pwd`/dep/${prefix}yaml-cpp/lib"
)

# Dependencies (shouldn't be modified)
lib=(
    "stdc++fs" "yaml-cpp" "pthread"
)

# yaml-cpp version
yaml_cpp_version=0.6.2
